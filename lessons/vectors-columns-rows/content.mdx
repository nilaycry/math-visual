---
title: "a vector, a column, a row"
description: "what's the same object and what isn't"
tags: ["linear algebra"]
color: "from-slate-500 to-zinc-500"
icon: "→"
date: "2026-04-16"
type: "connection"
draft: true
---

most courses introduce vectors, then column vectors, then row vectors, and treat
them as basically the same thing written differently. they aren't. this note is
about what's actually going on.

---

### the vector

[TODO: start with the abstract object. a vector is an element of a vector space.
no coordinates yet. direction and magnitude, or just an arrow. the point is that
a vector exists before you pick a basis.]

---

### the column

[TODO: once you pick a basis, you can write a vector as a column of coordinates.
the column is a representation, not the vector itself. changing basis changes the
column but not the vector. this distinction is easy to miss and causes confusion later.]

---

### the row

[TODO: a row vector is not a vector written sideways. it's a different kind of object:
a linear function from vectors to scalars. aᵀ doesn't live in the same space as a.
it lives in the dual space. this is the key point most first courses skip entirely.
connect to the dot product note: aᵀb is aᵀ acting on b as a function.]

---

### why it matters

[TODO: coordinate changes, the difference between contravariant and covariant components,
why physicists and mathematicians sometimes seem to be saying different things about
the same object. keep it concrete and short.]

---

### what this connects to

[TODO: point to row-vectors-are-functions for the full dual space story.
point to dot product note for the matrix multiplication angle.]
